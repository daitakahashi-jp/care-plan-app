<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>訪問介護計画書 自動入力システム</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;400;500;700&family=IBM+Plex+Mono:wght@400;600&display=swap');

  :root {
    --bg: #0d1117;
    --surface: #161b22;
    --surface2: #1c2128;
    --border: #30363d;
    --border2: #21262d;
    --accent: #2f81f7;
    --accent2: #388bfd;
    --accent-dim: rgba(47,129,247,0.15);
    --green: #3fb950;
    --green-dim: rgba(63,185,80,0.15);
    --yellow: #d29922;
    --yellow-dim: rgba(210,153,34,0.15);
    --red: #f85149;
    --red-dim: rgba(248,81,73,0.12);
    --purple: #a371f7;
    --text: #e6edf3;
    --text2: #8b949e;
    --text3: #6e7681;
    --radius: 8px;
    --mono: 'IBM Plex Mono', monospace;
    --sans: 'Noto Sans JP', sans-serif;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--sans);
    font-size: 14px;
    line-height: 1.6;
    min-height: 100vh;
  }

  /* ===== TOP NAV ===== */
  .topbar {
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    padding: 0 24px;
    height: 56px;
    display: flex;
    align-items: center;
    gap: 16px;
    position: sticky;
    top: 0;
    z-index: 100;
  }
  .topbar-logo {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 700;
    font-size: 15px;
    letter-spacing: -0.3px;
  }
  .topbar-logo .dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 8px var(--accent);
  }
  .topbar-sub { color: var(--text2); font-size: 12px; margin-left: auto; font-family: var(--mono); }

  /* ===== LAYOUT ===== */
  .layout {
    display: grid;
    grid-template-columns: 280px 1fr;
    min-height: calc(100vh - 56px);
  }
  .sidebar {
    background: var(--surface);
    border-right: 1px solid var(--border);
    padding: 20px 0;
    position: sticky;
    top: 56px;
    height: calc(100vh - 56px);
    overflow-y: auto;
  }
  .sidebar-section { padding: 0 16px; margin-bottom: 24px; }
  .sidebar-label {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: var(--text3);
    margin-bottom: 8px;
    padding: 0 4px;
  }
  .step-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 10px;
    border-radius: 6px;
    cursor: pointer;
    transition: background 0.15s;
    margin-bottom: 2px;
  }
  .step-item:hover { background: var(--surface2); }
  .step-item.active { background: var(--accent-dim); }
  .step-item.active .step-num { background: var(--accent); color: #fff; }
  .step-item.done .step-num { background: var(--green); color: #fff; }
  .step-num {
    width: 22px; height: 22px;
    border-radius: 50%;
    background: var(--border);
    color: var(--text2);
    font-size: 11px;
    font-weight: 700;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
    font-family: var(--mono);
  }
  .step-text { font-size: 13px; color: var(--text2); }
  .step-item.active .step-text { color: var(--text); font-weight: 500; }

  /* ===== MAIN ===== */
  .main { padding: 32px; overflow-y: auto; }
  .page { display: none; }
  .page.active { display: block; animation: fadeIn 0.2s ease; }
  @keyframes fadeIn { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); } }

  .page-header { margin-bottom: 28px; }
  .page-title { font-size: 22px; font-weight: 700; letter-spacing: -0.5px; margin-bottom: 6px; }
  .page-desc { color: var(--text2); font-size: 13px; }

  /* ===== CARDS ===== */
  .card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 20px;
    margin-bottom: 16px;
  }
  .card-title {
    font-size: 13px;
    font-weight: 600;
    color: var(--text2);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    margin-bottom: 14px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .card-title::before {
    content: '';
    display: block;
    width: 3px; height: 14px;
    background: var(--accent);
    border-radius: 2px;
  }

  /* ===== UPLOAD ZONE ===== */
  .upload-zone {
    border: 2px dashed var(--border);
    border-radius: var(--radius);
    padding: 48px 24px;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
    position: relative;
  }
  .upload-zone:hover, .upload-zone.drag-over {
    border-color: var(--accent);
    background: var(--accent-dim);
  }
  .upload-zone input[type=file] {
    position: absolute; inset: 0;
    opacity: 0; cursor: pointer;
    width: 100%; height: 100%;
  }
  .upload-icon {
    font-size: 36px;
    margin-bottom: 12px;
    display: block;
  }
  .upload-title { font-size: 15px; font-weight: 600; margin-bottom: 6px; }
  .upload-sub { color: var(--text2); font-size: 12px; }
  .upload-badge {
    display: inline-block;
    background: var(--green-dim);
    color: var(--green);
    border: 1px solid var(--green);
    border-radius: 4px;
    padding: 2px 8px;
    font-size: 11px;
    font-family: var(--mono);
    margin-top: 8px;
  }

  /* ===== TABLE ===== */
  .table-wrap { overflow-x: auto; border-radius: var(--radius); }
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 12px;
  }
  th {
    background: var(--surface2);
    color: var(--text2);
    font-weight: 600;
    padding: 8px 12px;
    text-align: left;
    border-bottom: 1px solid var(--border);
    white-space: nowrap;
  }
  td {
    padding: 7px 12px;
    border-bottom: 1px solid var(--border2);
    color: var(--text);
  }
  tr:last-child td { border-bottom: none; }
  tr:hover td { background: var(--surface2); }
  .badge {
    display: inline-block;
    padding: 1px 7px;
    border-radius: 4px;
    font-size: 11px;
    font-weight: 600;
    font-family: var(--mono);
  }
  .badge-body { background: var(--accent-dim); color: var(--accent2); }
  .badge-life { background: var(--green-dim); color: var(--green); }

  /* ===== FORM ===== */
  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }
  .form-group { display: flex; flex-direction: column; gap: 4px; }
  .form-group.full { grid-column: 1 / -1; }
  label { font-size: 12px; color: var(--text2); font-weight: 500; }
  input, select, textarea {
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 8px 10px;
    color: var(--text);
    font-family: var(--sans);
    font-size: 13px;
    transition: border-color 0.15s;
    outline: none;
  }
  input:focus, select:focus, textarea:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px var(--accent-dim);
  }
  textarea { resize: vertical; min-height: 72px; }

  /* ===== BUTTONS ===== */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 16px;
    border-radius: 6px;
    border: none;
    cursor: pointer;
    font-size: 13px;
    font-weight: 600;
    font-family: var(--sans);
    transition: all 0.15s;
    text-decoration: none;
  }
  .btn-primary {
    background: var(--accent);
    color: #fff;
  }
  .btn-primary:hover { background: var(--accent2); }
  .btn-secondary {
    background: var(--surface2);
    color: var(--text);
    border: 1px solid var(--border);
  }
  .btn-secondary:hover { background: var(--border); }
  .btn-success {
    background: var(--green);
    color: #fff;
  }
  .btn-lg { padding: 10px 22px; font-size: 14px; }
  .btn-row { display: flex; gap: 10px; margin-top: 16px; flex-wrap: wrap; }

  /* ===== AI SECTION ===== */
  .ai-box {
    background: linear-gradient(135deg, rgba(47,129,247,0.08) 0%, rgba(163,113,247,0.08) 100%);
    border: 1px solid rgba(47,129,247,0.3);
    border-radius: var(--radius);
    padding: 20px;
    margin-bottom: 16px;
  }
  .ai-header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 14px;
  }
  .ai-icon {
    width: 32px; height: 32px;
    background: var(--accent);
    border-radius: 8px;
    display: flex; align-items: center; justify-content: center;
    font-size: 16px;
    flex-shrink: 0;
  }
  .ai-label { font-size: 13px; font-weight: 700; color: var(--accent2); }
  .ai-sub { font-size: 11px; color: var(--text3); }

  .spinner {
    display: none;
    width: 18px; height: 18px;
    border: 2px solid var(--border);
    border-top-color: var(--accent);
    border-radius: 50%;
    animation: spin 0.6s linear infinite;
  }
  @keyframes spin { to { transform: rotate(360deg); } }

  .result-box {
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 14px;
    font-size: 13px;
    line-height: 1.7;
    white-space: pre-wrap;
    min-height: 80px;
    color: var(--text);
    margin-top: 12px;
    display: none;
  }
  .result-box.visible { display: block; }

  /* ===== PLAN PREVIEW ===== */
  .plan-section {
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    margin-bottom: 16px;
  }
  .plan-section-header {
    background: var(--surface);
    padding: 10px 16px;
    font-weight: 600;
    font-size: 13px;
    border-bottom: 1px solid var(--border);
    color: var(--text2);
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .plan-field {
    display: grid;
    grid-template-columns: 200px 1fr;
    border-bottom: 1px solid var(--border2);
  }
  .plan-field:last-child { border-bottom: none; }
  .plan-field-label {
    padding: 10px 16px;
    font-size: 12px;
    color: var(--text2);
    background: rgba(255,255,255,0.02);
    border-right: 1px solid var(--border2);
    font-weight: 500;
  }
  .plan-field-value {
    padding: 10px 16px;
    font-size: 13px;
  }

  /* ===== PROGRESS ===== */
  .progress-steps {
    display: flex;
    gap: 0;
    margin-bottom: 28px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    padding: 16px 20px;
  }
  .ps-item {
    flex: 1;
    text-align: center;
    position: relative;
  }
  .ps-item:not(:last-child)::after {
    content: '→';
    position: absolute;
    right: -8px;
    top: 8px;
    color: var(--text3);
    font-size: 14px;
  }
  .ps-num {
    width: 28px; height: 28px;
    border-radius: 50%;
    background: var(--border);
    color: var(--text2);
    font-size: 12px;
    font-weight: 700;
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 4px;
    font-family: var(--mono);
  }
  .ps-item.active .ps-num { background: var(--accent); color: #fff; }
  .ps-item.done .ps-num { background: var(--green); color: #fff; }
  .ps-label { font-size: 10px; color: var(--text3); }
  .ps-item.active .ps-label { color: var(--text2); }

  /* ===== STATUS BADGE ===== */
  .status-row {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    background: var(--surface2);
    border-radius: 6px;
    margin-bottom: 12px;
    font-size: 12px;
    border: 1px solid var(--border2);
  }
  .status-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--text3);
  }
  .status-dot.ok { background: var(--green); box-shadow: 0 0 6px var(--green); }
  .status-dot.warn { background: var(--yellow); box-shadow: 0 0 6px var(--yellow); }

  /* ===== WEEKLY GRID ===== */
  .weekly-table table { table-layout: fixed; }
  .weekly-table th, .weekly-table td {
    text-align: center;
    padding: 6px 4px;
    font-size: 12px;
  }
  .weekly-table td.has-service {
    background: var(--accent-dim);
    color: var(--accent2);
    font-weight: 600;
  }
  .weekly-table td.content-col {
    text-align: left;
    padding-left: 10px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 220px;
  }

  /* ===== SCROLLBAR ===== */
  ::-webkit-scrollbar { width: 6px; height: 6px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--text3); }

  /* ===== EXPORT SECTION ===== */
  .export-card {
    background: linear-gradient(135deg, var(--surface) 0%, var(--surface2) 100%);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 24px;
    text-align: center;
  }
  .export-icon { font-size: 48px; margin-bottom: 12px; }
  .export-title { font-size: 18px; font-weight: 700; margin-bottom: 6px; }
  .export-sub { color: var(--text2); font-size: 13px; margin-bottom: 20px; }

  .tags { display: flex; flex-wrap: wrap; gap: 6px; }
  .tag {
    display: inline-block;
    padding: 2px 8px;
    border-radius: 4px;
    font-size: 11px;
    background: var(--surface2);
    border: 1px solid var(--border);
    color: var(--text2);
  }

  select option { background: var(--surface2); }

  /* Notice */
  .notice {
    background: var(--yellow-dim);
    border: 1px solid var(--yellow);
    border-radius: 6px;
    padding: 10px 14px;
    font-size: 12px;
    color: var(--text);
    margin-bottom: 14px;
    display: flex;
    gap: 8px;
    align-items: flex-start;
  }
</style>
</head>
<body>

<!-- TOPBAR -->
<div class="topbar">
  <div class="topbar-logo">
    <div class="dot"></div>
    訪問介護計画書 自動入力システム
  </div>
  <span class="topbar-sub">KAIPOKE CSV → LLM → 計画書</span>
</div>

<!-- LAYOUT -->
<div class="layout">
  <!-- SIDEBAR -->
  <nav class="sidebar">
    <div class="sidebar-section">
      <div class="sidebar-label">ワークフロー</div>
      <div class="step-item active" onclick="gotoPage(0, this)">
        <div class="step-num">1</div>
        <div class="step-text">CSVアップロード</div>
      </div>
      <div class="step-item" onclick="gotoPage(1, this)">
        <div class="step-num">2</div>
        <div class="step-text">記録データ確認</div>
      </div>
      <div class="step-item" onclick="gotoPage(2, this)">
        <div class="step-num">3</div>
        <div class="step-text">利用者情報入力</div>
      </div>
      <div class="step-item" onclick="gotoPage(3, this)">
        <div class="step-num">4</div>
        <div class="step-text">AI要約・計画生成</div>
      </div>
      <div class="step-item" onclick="gotoPage(4, this)">
        <div class="step-num">5</div>
        <div class="step-text">計画書プレビュー</div>
      </div>
      <div class="step-item" onclick="gotoPage(5, this)">
        <div class="step-num">6</div>
        <div class="step-text">エクスポート</div>
      </div>
    </div>

    <div class="sidebar-section">
      <div class="sidebar-label">状態</div>
      <div class="status-row">
        <div class="status-dot" id="dot-csv"></div>
        <span id="lbl-csv">CSV 未読み込み</span>
      </div>
      <div class="status-row">
        <div class="status-dot" id="dot-info"></div>
        <span id="lbl-info">利用者情報 未入力</span>
      </div>
      <div class="status-row">
        <div class="status-dot" id="dot-ai"></div>
        <span id="lbl-ai">AI生成 未実行</span>
      </div>
    </div>
  </nav>

  <!-- MAIN -->
  <main class="main">
    <!-- ===== STEP 1: UPLOAD ===== -->
    <div class="page active" id="page-0">
      <div class="page-header">
        <div class="page-title">① カイポケCSVのアップロード</div>
        <div class="page-desc">カイポケからダウンロードした訪問介護記録CSVを読み込みます</div>
      </div>

      <div class="card">
        <div class="card-title">CSVファイルを選択</div>
        <div class="upload-zone" id="uploadZone">
          <input type="file" id="csvFile" accept=".csv">
          <span class="upload-icon">📂</span>
          <div class="upload-title">ファイルをドラッグ＆ドロップ</div>
          <div class="upload-sub">またはクリックして選択</div>
          <div class="upload-badge">.csv のみ対応</div>
        </div>
      </div>

      <div class="card">
        <div class="card-title">カイポケCSVの列仕様（対応フォーマット）</div>
        <div class="table-wrap">
          <table>
            <tr>
              <th>列名</th><th>内容</th><th>例</th>
            </tr>
            <tr><td>日付</td><td>サービス実施日</td><td>2025/06/02</td></tr>
            <tr><td>曜日</td><td>月〜日</td><td>月</td></tr>
            <tr><td>利用者名</td><td>フルネーム</td><td>山田 花子</td></tr>
            <tr><td>サービス種別</td><td>身体介護 / 生活援助</td><td>身体介護</td></tr>
            <tr><td>サービス内容</td><td>具体的な援助内容</td><td>起床介助・更衣介助</td></tr>
            <tr><td>開始時刻</td><td>HH:MM形式</td><td>09:00</td></tr>
            <tr><td>終了時刻</td><td>HH:MM形式</td><td>10:00</td></tr>
            <tr><td>提供時間(分)</td><td>数値</td><td>60</td></tr>
            <tr><td>担当ヘルパー</td><td>氏名</td><td>田中 美咲</td></tr>
            <tr><td>利用者状態・申送り事項</td><td>自由記述</td><td>体調良好。笑顔あり。</td></tr>
          </table>
        </div>
      </div>

      <div class="notice">
        💡 サンプルデータ（ダミー4週間分CSV）を読み込んでテストすることもできます。「サンプルデータを使用」をクリックしてください。
      </div>
      <div class="btn-row">
        <button class="btn btn-secondary" onclick="loadSampleData()">📄 サンプルデータを使用</button>
      </div>
    </div>

    <!-- ===== STEP 2: DATA CHECK ===== -->
    <div class="page" id="page-1">
      <div class="page-header">
        <div class="page-title">② 記録データ確認</div>
        <div class="page-desc">読み込んだCSVデータを確認してください</div>
      </div>

      <div class="card">
        <div class="card-title">読み込み結果サマリ</div>
        <div id="summaryArea"></div>
      </div>

      <div class="card">
        <div class="card-title">週別サービス分布</div>
        <div id="weeklyChart"></div>
      </div>

      <div class="card">
        <div class="card-title">記録データ一覧（先頭20件）</div>
        <div class="table-wrap" id="dataTable"></div>
      </div>

      <div class="btn-row">
        <button class="btn btn-primary btn-lg" onclick="gotoPageNum(2)">次へ：利用者情報入力 →</button>
      </div>
    </div>

    <!-- ===== STEP 3: USER INFO ===== -->
    <div class="page" id="page-2">
      <div class="page-header">
        <div class="page-title">③ 利用者・事業所情報の入力</div>
        <div class="page-desc">訪問介護計画書に記載する基本情報を入力します</div>
      </div>

      <div class="card">
        <div class="card-title">利用者情報</div>
        <div class="form-grid">
          <div class="form-group">
            <label>利用者名</label>
            <input type="text" id="f_name" value="山田 花子">
          </div>
          <div class="form-group">
            <label>性別</label>
            <select id="f_gender"><option>女</option><option>男</option></select>
          </div>
          <div class="form-group">
            <label>生年月日</label>
            <input type="text" id="f_birth" value="昭和22年5月10日">
          </div>
          <div class="form-group">
            <label>要介護度</label>
            <select id="f_care_level">
              <option>要介護1</option><option>要介護2</option>
              <option selected>要介護3</option><option>要介護4</option><option>要介護5</option>
            </select>
          </div>
          <div class="form-group full">
            <label>住所</label>
            <input type="text" id="f_addr" value="東京都新宿区〇〇町1-2-3">
          </div>
        </div>
      </div>

      <div class="card">
        <div class="card-title">緊急連絡先</div>
        <div class="form-grid">
          <div class="form-group">
            <label>緊急連絡先①（氏名）</label>
            <input type="text" id="f_emg1_name" value="山田 太郎">
          </div>
          <div class="form-group">
            <label>続柄</label>
            <input type="text" id="f_emg1_rel" value="長男">
          </div>
          <div class="form-group">
            <label>電話</label>
            <input type="text" id="f_emg1_tel" value="090-1234-5678">
          </div>
          <div class="form-group">
            <label>緊急連絡先②（氏名）</label>
            <input type="text" id="f_emg2_name" value="山田 幸子">
          </div>
          <div class="form-group">
            <label>続柄</label>
            <input type="text" id="f_emg2_rel" value="長女">
          </div>
          <div class="form-group">
            <label>電話</label>
            <input type="text" id="f_emg2_tel" value="090-8765-4321">
          </div>
        </div>
      </div>

      <div class="card">
        <div class="card-title">事業所情報</div>
        <div class="form-grid">
          <div class="form-group">
            <label>事業所名</label>
            <input type="text" id="f_office" value="○○訪問介護ステーション">
          </div>
          <div class="form-group">
            <label>管理者名</label>
            <input type="text" id="f_manager" value="高橋 一郎">
          </div>
          <div class="form-group">
            <label>サービス提供責任者</label>
            <input type="text" id="f_svcresp" value="中村 恵子">
          </div>
          <div class="form-group">
            <label>主たる担当ヘルパー</label>
            <input type="text" id="f_helper" value="田中 美咲">
          </div>
          <div class="form-group">
            <label>ケアマネ氏名</label>
            <input type="text" id="f_cm_name" value="小林 真由子">
          </div>
          <div class="form-group">
            <label>ケアマネ所属</label>
            <input type="text" id="f_cm_office" value="○○居宅介護支援事業所">
          </div>
          <div class="form-group">
            <label>主治医</label>
            <input type="text" id="f_doctor" value="鈴木 誠 医師">
          </div>
          <div class="form-group">
            <label>計画期間</label>
            <input type="text" id="f_period" value="2025年6月1日 〜 2025年11月30日">
          </div>
        </div>
      </div>

      <div class="btn-row">
        <button class="btn btn-primary btn-lg" onclick="saveInfoAndNext()">次へ：AI要約・計画生成 →</button>
      </div>
    </div>

    <!-- ===== STEP 4: AI ===== -->
    <div class="page" id="page-3">
      <div class="page-header">
        <div class="page-title">④ AI要約・援助計画生成</div>
        <div class="page-desc">カイポケ記録をLLMが分析し、計画書用の文章を生成します</div>
      </div>

      <div class="ai-box">
        <div class="ai-header">
          <div class="ai-icon">🤖</div>
          <div>
            <div class="ai-label">Claude AI による自動生成</div>
            <div class="ai-sub">4週間の介護記録を分析して援助目標・留意事項・週間計画を生成します</div>
          </div>
          <div class="spinner" id="spinner1"></div>
        </div>
        <button class="btn btn-primary btn-lg" onclick="runAI()" id="aiBtn">
          ✨ AI生成を開始
        </button>
      </div>

      <div class="card" id="aiResultCard" style="display:none">
        <div class="card-title">生成結果</div>
        <div class="result-box visible" id="aiRawResult"></div>
        <div class="btn-row">
          <button class="btn btn-success btn-lg" onclick="applyAIResult()">✅ この内容を計画書に適用する</button>
          <button class="btn btn-secondary" onclick="runAI()">🔄 再生成</button>
        </div>
      </div>

      <div class="card">
        <div class="card-title">手動編集（AI生成後に調整可能）</div>
        <div class="form-grid">
          <div class="form-group full">
            <label>長期目標</label>
            <textarea id="f_long_goal" rows="2"></textarea>
          </div>
          <div class="form-group full">
            <label>短期目標①</label>
            <textarea id="f_short_goal1" rows="2"></textarea>
          </div>
          <div class="form-group full">
            <label>短期目標②</label>
            <textarea id="f_short_goal2" rows="2"></textarea>
          </div>
          <div class="form-group full">
            <label>留意事項（改行で複数）</label>
            <textarea id="f_notes" rows="4"></textarea>
          </div>
        </div>
      </div>

      <div class="btn-row">
        <button class="btn btn-primary btn-lg" onclick="gotoPageNum(4)">次へ：計画書プレビュー →</button>
      </div>
    </div>

    <!-- ===== STEP 5: PREVIEW ===== -->
    <div class="page" id="page-4">
      <div class="page-header">
        <div class="page-title">⑤ 訪問介護計画書プレビュー</div>
        <div class="page-desc">入力内容を確認してください</div>
      </div>

      <div id="previewContent"></div>

      <div class="btn-row">
        <button class="btn btn-success btn-lg" onclick="gotoPageNum(5)">次へ：エクスポート →</button>
        <button class="btn btn-secondary" onclick="gotoPageNum(2)">← 情報を修正する</button>
      </div>
    </div>

    <!-- ===== STEP 6: EXPORT ===== -->
    <div class="page" id="page-5">
      <div class="page-header">
        <div class="page-title">⑥ エクスポート</div>
        <div class="page-desc">訪問介護計画書データをダウンロードします</div>
      </div>

      <div class="export-card">
        <div class="export-icon">📋</div>
        <div class="export-title">計画書データのエクスポート</div>
        <div class="export-sub">以下の形式でデータをダウンロードできます</div>
        <div class="btn-row" style="justify-content:center">
          <button class="btn btn-primary btn-lg" onclick="exportCSV()">📥 CSV ダウンロード</button>
          <button class="btn btn-secondary btn-lg" onclick="exportJSON()">📄 JSON ダウンロード</button>
          <button class="btn btn-secondary btn-lg" onclick="printPlan()">🖨️ 印刷プレビュー</button>
        </div>
      </div>

      <div class="card" style="margin-top:16px">
        <div class="card-title">このシステムの全体フロー</div>
        <div style="color:var(--text2); font-size:13px; line-height:2;">
          <div>① カイポケから訪問介護記録をCSVでダウンロード</div>
          <div style="color:var(--text3); padding-left:16px; font-size:12px;">▼</div>
          <div>② このシステムにCSVをアップロード</div>
          <div style="color:var(--text3); padding-left:16px; font-size:12px;">▼</div>
          <div>③ 利用者・事業所のマスター情報を入力（初回のみ）</div>
          <div style="color:var(--text3); padding-left:16px; font-size:12px;">▼</div>
          <div>④ Claude AIが記録を分析 → 援助目標・留意事項を自動生成</div>
          <div style="color:var(--text3); padding-left:16px; font-size:12px;">▼</div>
          <div>⑤ 担当者が内容を確認・修正</div>
          <div style="color:var(--text3); padding-left:16px; font-size:12px;">▼</div>
          <div>⑥ 計画書データをエクスポートし、Excelやシステムへ転記</div>
        </div>
      </div>
    </div>
  </main>
</div>

<script>
// ===== STATE =====
const state = {
  records: [],
  userInfo: {},
  aiResult: null,
};

const SAMPLE_CSV = `日付,曜日,利用者名,サービス種別,サービス内容,開始時刻,終了時刻,提供時間(分),担当ヘルパー,利用者状態・申送り事項,完了
2025/06/02,月,山田 花子,身体介護,起床介助・更衣介助,09:00,10:00,60,田中 美咲,本日は体調良好。笑顔あり。,○
2025/06/02,月,山田 花子,生活援助,調理（昼食・夕食）,15:00,16:30,90,田中 美咲,食欲良好。全量摂取。,○
2025/06/03,火,山田 花子,身体介護,入浴介助,09:00,10:00,60,鈴木 由美,体調良好。会話も弾む。,○
2025/06/04,水,山田 花子,身体介護,排泄介助,09:00,09:45,45,田中 美咲,足のむくみあり。確認後申送り済み。,○
2025/06/04,水,山田 花子,生活援助,掃除（居室）,14:00,15:00,60,田中 美咲,機嫌良く、サービス全て完了。,○
2025/06/05,木,山田 花子,身体介護,入浴介助,09:00,10:00,60,佐藤 健一,血圧やや高め（140/88）。休息促す。,○
2025/06/06,金,山田 花子,身体介護,食事介助（朝食）,08:00,08:45,45,田中 美咲,少し疲れた様子。食欲やや低下。,○
2025/06/06,金,山田 花子,生活援助,買い物代行,15:00,16:30,90,田中 美咲,本日は体調良好。笑顔あり。,○
2025/06/09,月,山田 花子,身体介護,起床介助・更衣介助,09:00,10:00,60,田中 美咲,睡眠が浅かったとのこと。,○
2025/06/10,火,山田 花子,身体介護,排泄介助,09:00,09:30,30,鈴木 由美,排泄にやや時間を要したが完了。,○
2025/06/10,火,山田 花子,生活援助,洗濯・乾燥・収納,14:00,15:30,90,鈴木 由美,体調良好。会話も弾む。,○
2025/06/11,水,山田 花子,身体介護,服薬確認・介助,09:00,09:30,30,佐藤 健一,本日は体調良好。笑顔あり。,○
2025/06/12,木,山田 花子,身体介護,入浴介助,09:00,10:00,60,田中 美咲,微熱あり（37.2℃）。ご家族に連絡済み。,○
2025/06/13,金,山田 花子,生活援助,調理（昼食・夕食）,14:00,15:30,90,田中 美咲,食欲良好。全量摂取。,○
2025/06/14,土,山田 花子,身体介護,排泄介助,09:00,09:45,45,鈴木 由美,機嫌良く、サービス全て完了。,○
2025/06/16,月,山田 花子,身体介護,食事介助（朝食）,09:00,09:45,45,田中 美咲,体調良好。会話も弾む。,○
2025/06/17,火,山田 花子,身体介護,入浴介助,09:00,10:00,60,佐藤 健一,少し疲れた様子。食欲やや低下。,○
2025/06/18,水,山田 花子,生活援助,掃除（居室）,14:00,15:00,60,田中 美咲,本日は体調良好。笑顔あり。,○
2025/06/19,木,山田 花子,身体介護,排泄介助,09:00,09:30,30,田中 美咲,血圧やや高め（142/90）。申送り済み。,○
2025/06/20,金,山田 花子,生活援助,調理（昼食・夕食）,14:00,15:30,90,鈴木 由美,食欲良好。全量摂取。,○
2025/06/21,土,山田 花子,身体介護,起床介助・更衣介助,09:00,10:00,60,佐藤 健一,体調良好。,○
2025/06/23,月,山田 花子,身体介護,起床介助・更衣介助,09:00,10:00,60,田中 美咲,本日は体調良好。笑顔あり。,○
2025/06/24,火,山田 花子,身体介護,入浴介助,09:00,10:00,60,田中 美咲,体調良好。会話も弾む。,○
2025/06/24,火,山田 花子,生活援助,洗濯・乾燥・収納,14:00,15:30,90,田中 美咲,機嫌良く、サービス全て完了。,○
2025/06/25,水,山田 花子,身体介護,服薬確認・介助,09:00,09:30,30,鈴木 由美,睡眠が浅かったとのこと。,○
2025/06/26,木,山田 花子,身体介護,食事介助（夕食）,15:00,15:45,45,佐藤 健一,足のむくみあり。確認後申送り済み。,○
2025/06/27,金,山田 花子,生活援助,買い物代行,14:00,15:30,90,田中 美咲,体調良好。笑顔あり。,○`;

// ===== PAGE NAV =====
const pages = document.querySelectorAll('.page');
const steps = document.querySelectorAll('.step-item');

function gotoPage(idx, el) {
  pages.forEach(p => p.classList.remove('active'));
  steps.forEach(s => s.classList.remove('active'));
  pages[idx].classList.add('active');
  if (el) el.classList.add('active');
  else steps[idx].classList.add('active');
  if (idx === 4) buildPreview();
}
function gotoPageNum(idx) {
  gotoPage(idx, steps[idx]);
  steps.forEach((s, i) => { if (i < idx) s.classList.add('done'); });
}

// ===== CSV UPLOAD =====
document.getElementById('csvFile').addEventListener('change', function(e) {
  const file = e.target.files[0];
  if (!file) return;
  const reader = new FileReader();
  reader.onload = function(ev) { parseCSV(ev.target.result); };
  reader.readAsText(file, 'UTF-8');
});

const zone = document.getElementById('uploadZone');
zone.addEventListener('dragover', e => { e.preventDefault(); zone.classList.add('drag-over'); });
zone.addEventListener('dragleave', () => zone.classList.remove('drag-over'));
zone.addEventListener('drop', e => {
  e.preventDefault();
  zone.classList.remove('drag-over');
  const file = e.dataTransfer.files[0];
  if (file && file.name.endsWith('.csv')) {
    const reader = new FileReader();
    reader.onload = ev => parseCSV(ev.target.result);
    reader.readAsText(file, 'UTF-8');
  }
});

function loadSampleData() { parseCSV(SAMPLE_CSV); }

function parseCSV(text) {
  const lines = text.trim().split('\n');
  const headers = lines[0].split(',').map(h => h.trim().replace(/^"|"$/g, ''));
  const records = [];
  for (let i = 1; i < lines.length; i++) {
    const vals = lines[i].split(',').map(v => v.trim().replace(/^"|"$/g, ''));
    if (vals.length < 2) continue;
    const obj = {};
    headers.forEach((h, j) => obj[h] = vals[j] || '');
    records.push(obj);
  }
  state.records = records;
  document.getElementById('dot-csv').className = 'status-dot ok';
  document.getElementById('lbl-csv').textContent = `CSV読込完了 (${records.length}件)`;
  buildSummary();
  gotoPageNum(1);
}

function buildSummary() {
  const r = state.records;
  const body = r.filter(x => x['サービス種別'] === '身体介護').length;
  const life = r.filter(x => x['サービス種別'] === '生活援助').length;
  const helpers = [...new Set(r.map(x => x['担当ヘルパー']).filter(Boolean))];
  const totalMin = r.reduce((s, x) => s + (parseInt(x['提供時間(分)']) || 0), 0);

  document.getElementById('summaryArea').innerHTML = `
    <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:12px">
      ${card2('📊 総記録件数', r.length + '件')}
      ${card2('🏃 身体介護', body + '件')}
      ${card2('🏠 生活援助', life + '件')}
      ${card2('⏱️ 総提供時間', Math.floor(totalMin/60) + '時間' + (totalMin%60) + '分')}
    </div>
    <div style="margin-top:12px;font-size:12px;color:var(--text2)">
      担当ヘルパー: ${helpers.join('、')}
    </div>`;

  // Weekly distribution
  const days = ['月','火','水','木','金','土','日'];
  const byDay = {};
  days.forEach(d => byDay[d] = {body:0, life:0});
  r.forEach(x => {
    const d = x['曜日'];
    if (byDay[d]) {
      if (x['サービス種別'] === '身体介護') byDay[d].body++;
      else byDay[d].life++;
    }
  });
  let thtml = '<div class="table-wrap weekly-table"><table><tr><th>曜日</th>';
  days.forEach(d => thtml += `<th>${d}</th>`);
  thtml += '</tr><tr><td style="color:var(--accent2);font-weight:600">身体介護</td>';
  days.forEach(d => thtml += `<td class="${byDay[d].body > 0 ? 'has-service' : ''}">${byDay[d].body > 0 ? byDay[d].body : '-'}</td>`);
  thtml += '</tr><tr><td style="color:var(--green);font-weight:600">生活援助</td>';
  days.forEach(d => thtml += `<td style="${byDay[d].life > 0 ? 'background:var(--green-dim);color:var(--green);font-weight:600' : ''}">${byDay[d].life > 0 ? byDay[d].life : '-'}</td>`);
  thtml += '</tr></table></div>';
  document.getElementById('weeklyChart').innerHTML = thtml;

  // Data table
  const show = r.slice(0, 20);
  const cols = ['日付','曜日','サービス種別','サービス内容','開始時刻','終了時刻','提供時間(分)','担当ヘルパー'];
  let t = '<div class="table-wrap"><table><tr>';
  cols.forEach(c => t += `<th>${c}</th>`);
  t += '</tr>';
  show.forEach(row => {
    t += '<tr>';
    cols.forEach(c => {
      let v = row[c] || '-';
      if (c === 'サービス種別') v = `<span class="badge ${v === '身体介護' ? 'badge-body' : 'badge-life'}">${v}</span>`;
      t += `<td>${v}</td>`;
    });
    t += '</tr>';
  });
  t += '</table></div>';
  document.getElementById('dataTable').innerHTML = t;
}

function card2(title, val) {
  return `<div style="background:var(--surface2);border:1px solid var(--border);border-radius:6px;padding:14px 16px">
    <div style="font-size:11px;color:var(--text3);margin-bottom:4px">${title}</div>
    <div style="font-size:20px;font-weight:700;font-family:var(--mono)">${val}</div>
  </div>`;
}

// ===== SAVE INFO =====
function saveInfoAndNext() {
  state.userInfo = {
    name: v('f_name'), gender: v('f_gender'), birth: v('f_birth'),
    care_level: v('f_care_level'), addr: v('f_addr'),
    emg1_name: v('f_emg1_name'), emg1_rel: v('f_emg1_rel'), emg1_tel: v('f_emg1_tel'),
    emg2_name: v('f_emg2_name'), emg2_rel: v('f_emg2_rel'), emg2_tel: v('f_emg2_tel'),
    office: v('f_office'), manager: v('f_manager'), svcresp: v('f_svcresp'),
    helper: v('f_helper'), cm_name: v('f_cm_name'), cm_office: v('f_cm_office'),
    doctor: v('f_doctor'), period: v('f_period'),
  };
  document.getElementById('dot-info').className = 'status-dot ok';
  document.getElementById('lbl-info').textContent = '利用者情報 入力済み';
  gotoPageNum(3);
}
function v(id) { const e = document.getElementById(id); return e ? e.value : ''; }

// ===== AI =====
async function runAI() {
  if (state.records.length === 0) { alert('先にCSVを読み込んでください'); return; }
  const btn = document.getElementById('aiBtn');
  const spinner = document.getElementById('spinner1');
  btn.disabled = true;
  btn.textContent = '生成中...';
  spinner.style.display = 'block';

  // Summarize records for prompt
  const svcSummary = {};
  state.records.forEach(r => {
    const key = r['サービス内容'];
    if (!svcSummary[key]) svcSummary[key] = { count: 0, type: r['サービス種別'], days: new Set() };
    svcSummary[key].count++;
    svcSummary[key].days.add(r['曜日']);
  });
  const svcLines = Object.entries(svcSummary)
    .map(([k, v]) => `・${v.type}「${k}」${v.count}回（${[...v.days].join('・')}曜）`)
    .join('\n');

  const conditions = state.records.map(r => r['利用者状態・申送り事項']).filter(Boolean);
  const condSample = [...new Set(conditions)].slice(0, 10).join('\n');

  const ui = state.userInfo;
  const prompt = `以下は訪問介護の4週間の実績記録データです。この記録をもとに、訪問介護計画書に記載する「援助目標」と「留意事項」を日本語で作成してください。

【利用者情報】
・氏名：${ui.name || '山田 花子'}
・要介護度：${ui.care_level || '要介護3'}
・担当ヘルパー：${ui.helper || '田中 美咲'}

【4週間のサービス実績】
${svcLines}

【利用者状態・申送り記録（抜粋）】
${condSample}

以下のJSON形式のみで出力してください（他の文章不要）：
{
  "long_goal": "長期目標（1〜2文）",
  "short_goal1": "短期目標①（1文）",
  "short_goal2": "短期目標②（1文）",
  "notes": ["留意事項①", "留意事項②", "留意事項③", "留意事項④"]
}`;

  try {
    const res = await fetch('/api/generate', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ prompt })
    });
    const data = await res.json();
    const text = data.content?.map(c => c.text || '').join('') || '';
    
    // Parse JSON from response
    const jsonMatch = text.match(/\{[\s\S]*\}/);
    if (jsonMatch) {
      const parsed = JSON.parse(jsonMatch[0]);
      state.aiResult = parsed;
      const display = `【長期目標】\n${parsed.long_goal}\n\n【短期目標①】\n${parsed.short_goal1}\n\n【短期目標②】\n${parsed.short_goal2}\n\n【留意事項】\n${parsed.notes.map((n,i) => `${i+1}. ${n}`).join('\n')}`;
      document.getElementById('aiRawResult').textContent = display;
      document.getElementById('aiResultCard').style.display = 'block';
      document.getElementById('dot-ai').className = 'status-dot ok';
      document.getElementById('lbl-ai').textContent = 'AI生成 完了';
    } else {
      document.getElementById('aiRawResult').textContent = text;
      document.getElementById('aiResultCard').style.display = 'block';
    }
  } catch (err) {
    // Fallback: generate reasonable content based on records
    const fallback = generateFallback();
    state.aiResult = fallback;
    const display = `【長期目標】\n${fallback.long_goal}\n\n【短期目標①】\n${fallback.short_goal1}\n\n【短期目標②】\n${fallback.short_goal2}\n\n【留意事項】\n${fallback.notes.map((n,i) => `${i+1}. ${n}`).join('\n')}\n\n※ APIへの接続ができなかったため、記録データを元にサンプル内容を生成しました。`;
    document.getElementById('aiRawResult').textContent = display;
    document.getElementById('aiResultCard').style.display = 'block';
    document.getElementById('dot-ai').className = 'status-dot warn';
    document.getElementById('lbl-ai').textContent = 'AI生成（オフライン）';
  }

  btn.disabled = false;
  btn.textContent = '✨ AI生成を開始';
  spinner.style.display = 'none';
}

function generateFallback() {
  const bodyServices = [...new Set(state.records.filter(r => r['サービス種別'] === '身体介護').map(r => r['サービス内容']))];
  const lifeServices = [...new Set(state.records.filter(r => r['サービス種別'] === '生活援助').map(r => r['サービス内容']))];
  const hasBath = bodyServices.some(s => s.includes('入浴'));
  const hasToilet = bodyServices.some(s => s.includes('排泄'));
  const hasMeds = bodyServices.some(s => s.includes('服薬'));
  const ui = state.userInfo;
  return {
    long_goal: `安全・安心な在宅生活を継続し、可能な限り自立した日常生活を送ることができるよう支援する。`,
    short_goal1: hasBath ? `週2回の入浴介助を通じて清潔保持と皮膚トラブルの予防を図る。` : `身体介護を通じて安全で快適な日常生活を維持する。`,
    short_goal2: hasMeds ? `服薬管理と体調確認を継続し、健康状態の安定を図る。` : `生活援助の継続により清潔で安全な居住環境を維持する。`,
    notes: [
      `高血圧の既往があるため、サービス提供前後にバイタルサインを確認する。`,
      hasToilet ? `排泄介助時は転倒に注意し、安全に誘導する。` : `移動時は転倒リスクに配慮し、常に見守りを行う。`,
      `体調変化（発熱・食欲不振・むくみ等）は速やかにサービス提供責任者・ご家族・ケアマネへ報告する。`,
      `認知機能の変化に留意し、穏やかな声かけと丁寧な説明を心がける。`,
    ]
  };
}

function applyAIResult() {
  if (!state.aiResult) return;
  document.getElementById('f_long_goal').value = state.aiResult.long_goal || '';
  document.getElementById('f_short_goal1').value = state.aiResult.short_goal1 || '';
  document.getElementById('f_short_goal2').value = state.aiResult.short_goal2 || '';
  document.getElementById('f_notes').value = (state.aiResult.notes || []).join('\n');
  alert('計画書に適用しました。「手動編集」欄で内容を調整できます。');
}

// ===== PREVIEW =====
function buildPreview() {
  const ui = state.userInfo;
  const long_goal = v('f_long_goal');
  const short1 = v('f_short_goal1');
  const short2 = v('f_short_goal2');
  const notes = v('f_notes');

  // Weekly service summary from records
  const days = ['月','火','水','木','金','土','日'];
  const svcByContent = {};
  state.records.forEach(r => {
    const key = r['サービス内容'];
    if (!svcByContent[key]) svcByContent[key] = { type: r['サービス種別'], days: new Set() };
    svcByContent[key].days.add(r['曜日']);
  });

  let weeklyHtml = '<table style="width:100%;border-collapse:collapse;font-size:12px">';
  weeklyHtml += '<tr><th style="padding:6px 10px;background:var(--surface);border:1px solid var(--border);text-align:left">種別</th><th style="padding:6px 10px;background:var(--surface);border:1px solid var(--border);text-align:left">サービス内容</th>';
  days.forEach(d => weeklyHtml += `<th style="padding:6px;background:var(--surface);border:1px solid var(--border);text-align:center;width:36px">${d}</th>`);
  weeklyHtml += '</tr>';

  Object.entries(svcByContent).forEach(([content, info]) => {
    weeklyHtml += `<tr>
      <td style="padding:6px 10px;border:1px solid var(--border2)"><span class="badge ${info.type === '身体介護' ? 'badge-body' : 'badge-life'}">${info.type}</span></td>
      <td style="padding:6px 10px;border:1px solid var(--border2);font-size:12px">${content}</td>`;
    days.forEach(d => {
      const has = info.days.has(d);
      weeklyHtml += `<td style="padding:6px;border:1px solid var(--border2);text-align:center;${has ? 'background:var(--accent-dim);color:var(--accent2);font-weight:700' : 'color:var(--text3)'}">
        ${has ? '○' : ''}
      </td>`;
    });
    weeklyHtml += '</tr>';
  });
  weeklyHtml += '</table>';

  document.getElementById('previewContent').innerHTML = `
    <div class="plan-section">
      <div class="plan-section-header">👤 利用者基本情報</div>
      ${pf('利用者名', ui.name + ' 様')}
      ${pf('性別', ui.gender)}
      ${pf('生年月日', ui.birth)}
      ${pf('要介護度', ui.care_level)}
      ${pf('住所', ui.addr)}
      ${pf('緊急連絡先①', `${ui.emg1_name}（${ui.emg1_rel}） ${ui.emg1_tel}`)}
      ${ui.emg2_name ? pf('緊急連絡先②', `${ui.emg2_name}（${ui.emg2_rel}） ${ui.emg2_tel}`) : ''}
    </div>
    <div class="plan-section">
      <div class="plan-section-header">🏢 事業所情報</div>
      ${pf('事業所名', ui.office)}
      ${pf('管理者', ui.manager)}
      ${pf('サービス提供責任者', ui.svcresp)}
      ${pf('主たる担当ヘルパー', ui.helper)}
      ${pf('介護支援専門員', ui.cm_name + '（' + ui.cm_office + '）')}
      ${pf('主治医', ui.doctor)}
      ${pf('計画期間', ui.period)}
    </div>
    <div class="plan-section">
      <div class="plan-section-header">🎯 援助目標</div>
      ${pf('長期目標', long_goal || '（未入力）')}
      ${pf('短期目標①', short1 || '（未入力）')}
      ${pf('短期目標②', short2 || '（未入力）')}
    </div>
    <div class="plan-section">
      <div class="plan-section-header">⚠️ 留意事項</div>
      <div class="plan-field">
        <div class="plan-field-label">留意事項</div>
        <div class="plan-field-value" style="white-space:pre-wrap">${notes || '（未入力）'}</div>
      </div>
    </div>
    <div class="plan-section">
      <div class="plan-section-header">📅 週間サービス計画（記録より自動集計）</div>
      <div style="padding:12px;">${weeklyHtml}</div>
    </div>`;
}

function pf(label, val) {
  return `<div class="plan-field">
    <div class="plan-field-label">${label}</div>
    <div class="plan-field-value">${val || '-'}</div>
  </div>`;
}

// ===== EXPORT =====
function exportCSV() {
  const ui = state.userInfo;
  const rows = [
    ['項目', '内容'],
    ['利用者名', ui.name], ['性別', ui.gender], ['生年月日', ui.birth],
    ['要介護度', ui.care_level], ['住所', ui.addr],
    ['緊急連絡先①', `${ui.emg1_name}（${ui.emg1_rel}） ${ui.emg1_tel}`],
    ['緊急連絡先②', `${ui.emg2_name}（${ui.emg2_rel}） ${ui.emg2_tel}`],
    ['事業所名', ui.office], ['管理者', ui.manager],
    ['サービス提供責任者', ui.svcresp], ['担当ヘルパー', ui.helper],
    ['介護支援専門員', ui.cm_name], ['主治医', ui.doctor],
    ['計画期間', ui.period],
    ['', ''],
    ['長期目標', v('f_long_goal')],
    ['短期目標①', v('f_short_goal1')],
    ['短期目標②', v('f_short_goal2')],
    ['留意事項', v('f_notes').replace(/\n/g, '／')],
  ];
  const csv = rows.map(r => r.map(c => `"${(c||'').replace(/"/g,'""')}"`).join(',')).join('\n');
  download('訪問介護計画書データ.csv', '\uFEFF' + csv, 'text/csv');
}

function exportJSON() {
  const data = {
    userInfo: state.userInfo,
    goals: {
      long: v('f_long_goal'),
      short1: v('f_short_goal1'),
      short2: v('f_short_goal2'),
      notes: v('f_notes').split('\n').filter(Boolean),
    },
    weeklyServices: (() => {
      const svc = {};
      state.records.forEach(r => {
        const k = r['サービス内容'];
        if (!svc[k]) svc[k] = { type: r['サービス種別'], days: [] };
        if (!svc[k].days.includes(r['曜日'])) svc[k].days.push(r['曜日']);
      });
      return svc;
    })(),
    generatedAt: new Date().toISOString(),
  };
  download('訪問介護計画書データ.json', JSON.stringify(data, null, 2), 'application/json');
}

function download(filename, content, type) {
  const a = document.createElement('a');
  a.href = URL.createObjectURL(new Blob([content], { type }));
  a.download = filename;
  a.click();
}

function printPlan() {
  buildPreview();
  window.print();
}
</script>
</body>
</html>
